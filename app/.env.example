# ==============================================================================
# UCSD Crime Logs - Frontend Environment Variables Example
# ==============================================================================
#
# This file contains example environment variables for the React frontend.
# Copy this file to .env and fill in your actual values.
#
# IMPORTANT: These are PUBLIC variables that will be exposed in the browser!
# Only use the anonymous (anon) key, never the service role key!
# ==============================================================================

# ------------------------------------------------------------------------------
# Supabase Configuration (Frontend)
# ------------------------------------------------------------------------------
# These credentials are used by the React app to submit user crime reports.

# Your Supabase project URL
# Find this in: Supabase Dashboard > Settings > API > Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anonymous/public key (NOT the service role key!)
# Find this in: Supabase Dashboard > Settings > API > anon/public key
# This key is safe to expose publicly (it has limited permissions via RLS)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ------------------------------------------------------------------------------
# Notes
# ------------------------------------------------------------------------------
# - Vite uses the VITE_ prefix for environment variables
# - These variables are embedded in the build and visible in the browser
# - Only use the anon key here! The service role key should only be used
#   server-side (in GitHub Actions for the sync script)
# - After changing these values, restart your dev server (npm run dev)
# ------------------------------------------------------------------------------
